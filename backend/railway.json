{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacks": {
      "pythonVersion": "3.14"
    }
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthCheckPath": "/api/platform/health/",
    "startCommand": "python manage.py migrate && python manage.py loaddata backend/backup_checklist.sql --skip-checks && gunicorn config.wsgi:application --bind 0.0.0.0:8000"
  }
}