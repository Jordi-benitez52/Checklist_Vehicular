export const environment = {
  production: true,
  // ================================================================
  // CONFIGURACIÓN DE PRODUCCIÓN
  // ================================================================
  // IMPORTANTE: Antes de compilar para producción, asegúrate de que
  // esta URL apunte al servidor de producción correcto.
  //
  // Ejemplo para servidor de producción:
  //   - 'https://checklist.miempresa.com/api/platform'
  //
  // El técnico de IT debe configurar esto antes de instalar la app
  // en los dispositivos de los guardias.
  // ================================================================
  apiUrl: 'http://192.168.1.50:8000/api/platform'
};